# Personal Bio Website — Bento Grid

Это простой, но стильный одностраничный сайт-визитка, созданный с использованием чистого HTML и CSS. Дизайн выполнен в виде "бенто-сетки" (bento grid), что делает его современным и удобным для отображения основной информации о вас.

![Скриншот главной страницы](https://envs.sh/BND.jpg)

### ✨ Особенности

*   **Современный дизайн**: Элегантная и минималистичная бенто-сетка.
*   **Адаптивность**: Отлично выглядит на десктопах, планшетах и мобильных устройствах.
*   **Плавные анимации**: Приятные CSS-анимации при загрузке и переходах между страницами.
*   **Простота кастомизации**: Легко изменить под себя, даже без глубоких знаний в веб-разработке.
*   **Легковесность**: Никаких фреймворков, только чистый HTML и CSS, что обеспечивает максимальную скорость загрузки.

---

### 🚀 Как развернуть на своем сервере/домене

Чтобы разместить этот сайт-визитку у себя, вам понадобится веб-сервер (например, Nginx или Apache) и домен, направленный на IP-адрес вашего сервера.

#### Шаг 1: Клонирование репозитория

Подключитесь к вашему серверу по SSH и выполните следующую команду, чтобы скопировать файлы проекта. Рекомендуется делать это в директории `/var/www/`.

```bash
# Перейдите в директорию для веб-проектов
cd /var/www/

# Клонируйте репозиторий
git clone https://github.com/Rewixx-png/bio-project.git your-domain.com

# Перейдите в созданную папку
cd your-domain.com
```
*Не забудьте заменить `your-domain.com` на имя вашего домена.*

#### Шаг 2: Настройка веб-сервера (Пример для Nginx)

Nginx — это быстрый и популярный веб-сервер. Если он у вас не установлен, установите его (`sudo apt install nginx`).

1.  Создайте новый файл конфигурации для вашего сайта:
    ```bash
    sudo nano /etc/nginx/sites-available/your-domain.com
    ```

2.  Вставьте в него следующую базовую конфигурацию, заменив `your-domain.com` на ваше доменное имя и `/var/www/your-domain.com` на путь к вашему проекту:

    ```nginx
    server {
        listen 80;
        listen [::]:80;

        server_name your-domain.com www.your-domain.com;

        root /var/www/your-domain.com;
        index index.html;

        location / {
            try_files $uri $uri/ =404;
        }
    }
    ```

3.  Активируйте конфигурацию, создав символическую ссылку:
    ```bash
    sudo ln -s /etc/nginx/sites-available/your-domain.com /etc/nginx/sites-enabled/
    ```

4.  Проверьте конфигурацию Nginx на наличие ошибок и перезапустите его:
    ```bash
    sudo nginx -t
    sudo systemctl restart nginx
    ```

Теперь, если вы перейдете по вашему домену в браузере, вы должны увидеть сайт.

*Не забудьте также настроить DNS-записи вашего домена (A-запись), чтобы они указывали на IP-адрес вашего сервера.*

---

### 🎨 Кастомизация и настройка

Настроить сайт под себя очень просто. Вам нужно отредактировать только HTML-файлы.

1.  **Основная информация (`index.html`)**:
    *   **Имя и профессия**: Найдите блок `.profile-header` и измените `<h1>Rewix</h1>` и `<p>Anime Editor</p>`.
    *   **Аватар**: Замените ссылку `https://envs.sh/B9V.jpg` на ссылку на ваше изображение.
    *   **Статус**: Измените текст в теге `<p class="status">`.

2.  **Ссылки на соцсети (`index.html`)**:
    *   В блоке `.socials-card` измените `href` в ссылках `<a>` на ваши профили в Telegram, Discord и GitHub.

3.  **Контактный Email (`index.html`)**:
    *   В блоке `.contact-card` измените email в ссылке `<a>`.

4.  **Навыки (`index.html`)**:
    *   В блоке `.skills-card` отредактируйте список `<li>`, добавив или удалив свои навыки.

5.  **Страница "Обо мне" (`about.html`)**:
    *   Просто отредактируйте текст внутри тегов `<h1>` и `<p>`.

6.  **Страница проекта (`project.html`)**:
    *   Измените заголовок и описание вашего проекта, а также ссылку в кнопке.

---

### 📄 Лицензия

Этот проект распространяется под лицензией MIT. Вы можете свободно использовать, изменять и распространять его. Подробности см. в файле `LICENSE`.